import { StyleSheet } from 'react-native';

export const commentInteractionBarStyles = StyleSheet.create({
  container: {
    flexDirection: 'row',
    alignItems: 'center',
    paddingTop: 4,
    gap: 16,
  },
  button: {
    flexDirection: 'row',
    alignItems: 'center',
  },
});
