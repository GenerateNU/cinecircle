import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    flexDirection: "row",
    alignItems: "center",
    gap: 8,
  },
  icon: {
    fontSize: 24,
    color: "#000",
  },
  text: {
    fontSize: 18,
    fontWeight: "400",
    color: "#000",
  },
});
